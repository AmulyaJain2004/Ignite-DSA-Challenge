#include<bits/stdc++.h> // includes all the library and utilities of c++ using one line
using namespace std; // useful when we don't want to not write always std:: before cout and cin

void print()
{
    cout << "Amulya";
    return;
}

int main()
{
    int a;
    cin >> a;
    print();
    cout << a;
    return 0;
}